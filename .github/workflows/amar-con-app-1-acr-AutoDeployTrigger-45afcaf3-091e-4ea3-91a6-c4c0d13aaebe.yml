name: Trigger auto deployment for amar-con-app-1-acr

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/amar-con-app-1-acr-AutoDeployTrigger-45afcaf3-091e-4ea3-91a6-c4c0d13aaebe.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AMARCONAPP1ACR_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AMARCONAPP1ACR_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AMARCONAPP1ACR_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: amarreg1.azurecr.io
          registryUsername: ${{ secrets.AMARCONAPP1ACR_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.AMARCONAPP1ACR_REGISTRY_PASSWORD }}
          containerAppName: amar-con-app-1-acr
          resourceGroup: amar-con-env-1
          imageToBuild: amarreg1.azurecr.io/amar-con-app-1-acr:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


